# 雜事房 Landing Page

Next.js App Router + Tailwind CSS conversion of the original single-file landing page.

## Local Development

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Deploy

This project is ready for Vercel. Import the repository and use the default Next.js settings.
